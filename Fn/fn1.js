// Write a function that takes a user object
// and returns formatted string
// Test: formatUser({name: "Swapnil", age: 25}) → "Swapnil is 25 years old"

// function formatUser(user) {
//   return `${user.name} is ${user.age} years old`;
// }

// console.log(formatUser({ name: "Swapnil", age: 25 }));

// function formatUser({ name, age }) {
//   return `${name} is ${age} years old`;
// }
// console.log(formatUser({ name: "Swapnil", age: 25 }));
