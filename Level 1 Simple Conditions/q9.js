//Vowel or consonant

let useInput = 'a';

if (useInput == 'a' || useInput == 'e' || useInput == 'i' || useInput == 'o' || useInput == 'u') {
    console.log("The Input is Vowel.");
} else {
    console.log("The Input is Consonent.");
}